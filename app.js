const $=id=>document.getElementById(id),qs=s=>document.querySelector(s),page=document.body?.dataset?.page||'home',sleep=ms=>new Promise(r=>setTimeout(r,ms)),fmt=size=>size<1024?`${size} B`:size<1048576?`${(size/1024).toFixed(1)} KB`:`${(size/1048576).toFixed(1)} MB`,getRoom=()=>sessionStorage.getItem('droppoint-room')||'',setRoom=v=>sessionStorage.setItem('droppoint-room',v);let socket=null,room=getRoom(),pc=null,channel=null,initiator=false,connected=false,incoming=null,files=[],pendingCandidates=[];
function wsUrl(){return `${location.protocol==='https:'?'wss':'ws'}://${location.host}`}function setStatus(text,on){const el=$('status');if(!el)return;el.innerHTML=`<span class="status-dot"></span> ${text}`;el.className=`status ${on?'on':'off'}`}function notice(text,kind='info'){const el=$('roomStatus')||$('connectNotice');if(!el)return;el.className=`notice ${kind}`;el.innerHTML=text}function go(path){location.href=path}function parsed(v){try{return JSON.parse(v)}catch{return null}}
function connectSignal(){if(socket&&socket.readyState<=WebSocket.OPEN)return Promise.resolve();return new Promise((resolve,reject)=>{socket=new WebSocket(wsUrl());socket.onopen=()=>{setStatus('Sunucu hazır',true);resolve()};socket.onerror=()=>{setStatus('Bağlantı hatası',false);notice('Sunucuya bağlanılamadı. Sayfayı yenileyip tekrar dene.','error');reject(new Error('websocket'))};socket.onclose=()=>{connected=false;setStatus('Bağlantı kapandı',false);updateTransferButtons()};socket.onmessage=handleSignal})}
async function handleSignal(event){const m=parsed(event.data);if(!m)return;if(m.type==='created'||m.type==='joined'){room=m.room;setRoom(room);if(page==='room'){const result=$('roomResult');result?.classList.remove('hidden');$('roomCode').textContent=room;notice(m.type==='created'?'Oda hazır. Diğer cihazın bu kodla katılmasını bekliyoruz.':'Odaya katıldın. Diğer cihazla eşleşme bekleniyor.','success')}if(page==='room'&&m.type==='joined')await createPeer(false);return}if(m.type==='peer-joined'){await createPeer(true);return}if(m.type==='room-full'){notice('Bu oda zaten iki cihazla dolu.','error');return}if(m.type==='error'){notice(m.message||'Bir hata oluştu.','error');return}if(m.type==='offer'){if(!pc)await createPeer(false);await pc.setRemoteDescription(m.offer);for(const c of pendingCandidates.splice(0))try{await pc.addIceCandidate(c)}catch{}const answer=await pc.createAnswer();await pc.setLocalDescription(answer);socket.send(JSON.stringify({type:'answer',answer}));return}if(m.type==='answer'&&pc&&initiator){await pc.setRemoteDescription(m.answer);for(const c of pendingCandidates.splice(0))try{await pc.addIceCandidate(c)}catch{}return}if(m.type==='ice'&&m.candidate){if(pc?.remoteDescription)try{await pc.addIceCandidate(m.candidate)}catch{}else pendingCandidates.push(m.candidate)}}
async function createPeer(asInitiator){if(pc)pc.close();pendingCandidates=[];initiator=!!asInitiator;pc=new RTCPeerConnection({iceServers:[{urls:'stun:stun.l.google.com:19302'}]});pc.onicecandidate=e=>{if(e.candidate&&socket?.readyState===WebSocket.OPEN)socket.send(JSON.stringify({type:'ice',candidate:e.candidate}))};pc.onconnectionstatechange=()=>{const s=pc.connectionState;connected=s==='connected';setStatus(connected?'Cihaz bağlı':s==='connecting'?'Cihaz bağlanıyor':'Cihaz ayrıldı',connected);updateTransferButtons();if(connected)notice('Cihazlar bağlandı. Paylaşım hazır.','success')};pc.ondatachannel=e=>setupChannel(e.channel);if(initiator){setupChannel(pc.createDataChannel('droppoint'));const offer=await pc.createOffer();await pc.setLocalDescription(offer);socket.send(JSON.stringify({type:'offer',offer}))}}
function setupChannel(ch){channel=ch;ch.binaryType='arraybuffer';ch.onopen=()=>{connected=true;setStatus('Cihaz bağlı',true);updateTransferButtons();notice('Cihazlar bağlandı. Paylaşım hazır.','success')};ch.onclose=()=>{connected=false;setStatus('Cihaz ayrıldı',false);updateTransferButtons()};ch.onerror=()=>{connected=false;setStatus('Aktarım hatası',false);updateTransferButtons()};ch.onmessage=receiveMessage}
function updateTransferButtons(){if($('sendFile'))$('sendFile').disabled=!connected||!files.length;if($('sendText'))$('sendText').disabled=!connected||!$('text')?.value.trim()}
async function createRoom(){try{await connectSignal();socket.send(JSON.stringify({type:'create'}))}catch{}}async function joinRoom(){const code=$('roomInput')?.value.trim().toUpperCase()||'';if(!/^[A-Z0-9]{6}$/.test(code)){notice('6 haneli oda kodunu gir.','error');return}try{await connectSignal();socket.send(JSON.stringify({type:'join',room:code}))}catch{}}
async function initRoomPage(){const mode=new URLSearchParams(location.search).get('mode');$('create')?.addEventListener('click',createRoom);$('join')?.addEventListener('click',joinRoom);$('roomInput')?.addEventListener('keydown',e=>{if(e.key==='Enter')joinRoom()});$('copyRoom')?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(room);$('copyRoom').textContent='Kopyalandı';setTimeout(()=>$('copyRoom').textContent='Kodu kopyala',1400)}catch{}});await connectSignal().catch(()=>{});if(mode==='create')$('create')?.focus();else if(mode==='join')$('roomInput')?.focus()}
function initChoosePage(){if(!room){go('room.html');return}$('currentRoom')&&($('currentRoom').textContent=room)}function renderFiles(){const list=$('fileList');if(!list)return;list.innerHTML='';for(const f of files){const tpl=$('fileItem').content.cloneNode(true);tpl.querySelector('b').textContent=f.name;tpl.querySelector('small').textContent=fmt(f.size);list.appendChild(tpl)}}
async function sendText(){if(!channel||channel.readyState!=='open')return;const text=$('text').value.trim();if(!text)return;channel.send(JSON.stringify({kind:'text',text}));$('text').value='';$('text').dispatchEvent(new Event('input'));addReceived('Metin gönderildi',`${text.length} karakter`)}async function sendFiles(){if(!channel||channel.readyState!=='open'||!files.length)return;for(const file of [...files]){const id=crypto.randomUUID();channel.send(JSON.stringify({kind:'file-start',id,name:file.name,size:file.size,type:file.type||'application/octet-stream'}));let offset=0;while(offset<file.size){while(channel.bufferedAmount>4*1024*1024)await sleep(20);const chunk=await file.slice(offset,offset+64*1024).arrayBuffer();channel.send(chunk);offset+=chunk.byteLength}channel.send(JSON.stringify({kind:'file-end',id}));addReceived('Dosya gönderildi',`${file.name} · ${fmt(file.size)}`)}}
function addReceived(title,body,download){const box=$('received');if(!box)return;box.querySelector('.muted')?.remove();const row=document.createElement('article');row.className='received-item';const copy=document.createElement('div');copy.innerHTML='<b></b><p></p>';copy.querySelector('b').textContent=title;copy.querySelector('p').textContent=body;row.appendChild(copy);if(download){const a=document.createElement('a');a.className='m3-btn tonal small';a.href=download.url;a.download=download.name;a.textContent='İndir';row.appendChild(a)}box.prepend(row)}function receiveMessage(e){if(typeof e.data==='string'){const msg=parsed(e.data);if(!msg)return;if(msg.kind==='text')addReceived('Metin alındı',msg.text);if(msg.kind==='file-start')incoming={id:msg.id,name:msg.name,size:msg.size,type:msg.type,chunks:[],received:0};if(msg.kind==='file-end'&&incoming?.id===msg.id){const blob=new Blob(incoming.chunks,{type:incoming.type}),url=URL.createObjectURL(blob);addReceived('Dosya alındı',`${incoming.name} · ${fmt(incoming.size)}`,{url,name:incoming.name});incoming=null}}else if(incoming){incoming.chunks.push(e.data);incoming.received+=e.data.byteLength}}
async function initTransferPage(){if(!room){go('room.html');return}const type=new URLSearchParams(location.search).get('type')||'file';$('transferRoom').textContent=room;const isFile=type==='file';$('transferTitle').textContent=isFile?'Dosya gönder':'Metin gönder';$('transferLead').textContent=isFile?'Dosyaları seç, ardından diğer cihaza gönder.':'Metnini yaz, ardından diğer cihaza gönder.';$(isFile?'filePanel':'textPanel')?.classList.remove('hidden');if($('file')){$('file').onchange=()=>{files=[...$('file').files];renderFiles();updateTransferButtons()};const drop=qs('.drop');if(drop){['dragenter','dragover'].forEach(ev=>drop.addEventListener(ev,e=>{e.preventDefault();drop.classList.add('drag')}));['dragleave','drop'].forEach(ev=>drop.addEventListener(ev,e=>{e.preventDefault();drop.classList.remove('drag')}));drop.addEventListener('drop',e=>{files=[...e.dataTransfer.files];renderFiles();updateTransferButtons()})}}if($('text'))$('text').addEventListener('input',()=>{$('count').textContent=`${$('text').value.length.toLocaleString('tr-TR')} / 20.000`;updateTransferButtons()});$('sendText')?.addEventListener('click',sendText);$('sendFile')?.addEventListener('click',sendFiles);$('clear')?.addEventListener('click',()=>{$('received').innerHTML='<p class="muted">Henüz bir şey alınmadı.</p>'});$('newRoom')?.addEventListener('click',()=>{sessionStorage.removeItem('droppoint-room');go('room.html')});await connectSignal().catch(()=>{});if(socket?.readyState===WebSocket.OPEN){socket.send(JSON.stringify({type:'join',room}))}if(!pc)await createPeer(false);updateTransferButtons()}
if(page==='room')initRoomPage();else if(page==='choose')initChoosePage();else if(page==='transfer')initTransferPage();